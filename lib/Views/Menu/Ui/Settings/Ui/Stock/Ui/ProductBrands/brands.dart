import 'package:flutter/material.dart';

class ProductBrandsView extends StatelessWidget {
  const ProductBrandsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
