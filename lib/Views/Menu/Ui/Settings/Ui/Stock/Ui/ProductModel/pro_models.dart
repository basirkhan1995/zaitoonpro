import 'package:flutter/material.dart';

class ProductModelsView extends StatelessWidget {
  const ProductModelsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
